name: "Terraform Deploy with Security Check by Checkov"

on: 
  push:
    branches:
      - Lab
  pull_request:
    branches:
      - master

defaults:
  run:
    working-directory: ./Labs/Lab1/terraform/workload  # Đảm bảo đường dẫn đúng

jobs:
  terraform-checkov:
    runs-on: ubuntu-latest
    name: Validate code and security scan with Checkov

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }} 
      
      - name: Run Checkov
        uses: bridgecrewio/checkov-action@v12
        with:
          directory: ./Labs/Lab1/terraform/workload  # Cập nhật lại đường dẫn
          framework: terraform
          soft_fail: true

  terraform-apply:
    runs-on: ubuntu-latest
    name: Apply Terraform Changes
    environment: aws
    env: 
      ACCESS_KEY: ${{ secrets.AWS_ACCESS_KEY_ID }}
      SECRET_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      TF_VERSION: 1.9.6

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ env.ACCESS_KEY }}
          aws-secret-access-key: ${{ env.SECRET_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Install Checkov
        run: pip install checkov

      - name: List the full directory structure
        run: |
          echo "Current working directory: $(pwd)"
          ls -al /home/runner/work/NT548_DevOps/NT548_DevOps/
          ls -al ./Labs/Lab1/terraform/
          ls -al ./Labs/Lab1/terraform/workload

      - name: Run Checkov on Terraform code
        working-directory: ./Labs/Lab1/terraform/workload
        run: checkov -d . || true

      - name: Initialize Terraform
        working-directory: ./Labs/Lab1/terraform/workload
        run: terraform init

      - name: Validate Terraform Configuration
        working-directory: ./Labs/Lab1/terraform/workload
        run: terraform validate -no-color

      - name: Plan Terraform Changes
        working-directory: ./Labs/Lab1/terraform/workload
        run: terraform plan -out=tfplan

      - name: Apply Terraform Changes
        working-directory: ./Labs/Lab1/terraform/workload
        run: terraform apply --auto-approve

      - name: Clean Up
        working-directory: ./Labs/Lab1/terraform/workload
        run: rm -f tfplan
